"""mandala fleet porcelain — dispatch + present over flake.mandala."""

__version__ = "0.1.0"

# The aggregate schema version this CLI understands (flake.mandala
# .schemaVersion). Reject anything newer instead of misreading it.
SUPPORTED_SCHEMA_VERSION = 1
